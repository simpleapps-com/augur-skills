{
  "private": true,
  "name": "augur-skills-monorepo",
  "description": "Curated skills marketplace for Claude Code",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=9.0.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "test:coverage": "pnpm -r test:coverage",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "validate-skills": "node scripts/validate-skills.mjs",
    "prepare-publish": "node scripts/prepare-publish.mjs"
  }
}
