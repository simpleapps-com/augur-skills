# Bash Simplicity

MUST run each Bash command as a separate, simple call. MUST NOT chain with `&&`, `||`, pipes, or sub-shells. Use dedicated tools (Grep, Glob, Read, Edit) instead of Bash equivalents.

Load Skill("bash-simplicity") for full conventions before running Bash commands.
